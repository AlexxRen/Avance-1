@model IEnumerable<Avance_1.Models.Agente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Estado_Agente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Estado_Agente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.IdPersona)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rol.IdRol)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAgente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAgente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAgente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
