@model IEnumerable<Avance_1.Models.CamECU911>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ubicacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdZona)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ubicacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdZona)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCam">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCam">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCam">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
